# IPA Writing Guideline: Age Ranges
# Use "age X-Y" format for ranges, not "aged X-Y" or "ages X-Y"
extends: existence
message: "For age ranges, use 'age' (not 'aged' or 'ages'): '%s'"
level: error
nonword: true
tokens:
  - '\b(?:aged|ages)\s+\d+(?:[-–]\d+)?'
